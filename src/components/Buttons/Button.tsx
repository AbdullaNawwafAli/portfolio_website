"use client";

import React from "react";

const Button = () => {
  return (
    <button onClick={() => console.log("working")} className='bg-'>
      hwdwdwd
    </button>
  );
};

export default Button;
